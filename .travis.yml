language: julia

os:
  - linux
  - osx

julia:
  # - nightly
  - 0.6
  - 0.6.2

before_script:
 - julia -e 'Pkg.add("JLD2"); Pkg.add("SpecialFunctions");' #Pkg.add("PyPlot"
 # - julia -e 'ENV["PYTHON"]="";Pkg.build("PyCall");'

# script:
#   - julia test/test_grid.jl
#   - julia test/test_twodturb.jl
#   - julia test/test_utils.jl

script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia -e 'Pkg.clone(pwd())'
  - julia -e 'Pkg.test("FourierFlows")'

notifications:
  email: false
